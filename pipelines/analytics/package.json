{
  "name": "analytics-pipeline",
  "version": "1.0.0",
  "description": "Shared GA4 analytics pipeline — collects data, detects trends, generates insights, dispatches to other pipelines",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --business safebath",
    "test": "node -e \"require('./config'); console.log('config loads OK')\""
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "googleapis": "^144.0.0",
    "js-yaml": "^4.1.0"
  }
}
